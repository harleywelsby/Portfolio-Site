import { SectionTitle } from '../../shared/styles/styledComponents';
import { ResearchSectionWrapper } from './ResearchSection.styles';

function ResearchSection() {
  // TODO: Unimplemented

  return (
    <ResearchSectionWrapper className="hidden">
      <SectionTitle $colour="white">Research</SectionTitle>
    </ResearchSectionWrapper>
  );
}

export default ResearchSection;
